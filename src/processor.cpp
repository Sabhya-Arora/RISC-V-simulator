#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <bits/stdc++.h>
using namespace std;

int main(int argc, char * argv[]) {
    char *file;
    file = argv[1];
    FILE *fp = fopen(file, "r");
    char inp[8];
    while (fgets(inp, sizeof(inp), fp)) {
        
    }
    return 0;
}